# Cursor Rules - 隐私保护规则
# 严格禁止访问包含敏感信息的文件和目录

## 禁止访问的文件类型
- 日志文件 (*.log, logs/ 目录)
- 配置文件中的敏感信息
- 包含用户数据的文件
- 包含业务数据的文件
- 包含路径信息的文件
- 临时文件目录
- 输出文件目录

## 禁止访问的目录
- logs/
- output/
- captured/
- __pycache__/
- 环境依赖包/
- 任何包含用户数据的目录

## 禁止读取的内容
- 日志内容
- 用户路径信息
- 业务数据（如钢铁质量数据）
- 个人身份信息
- 文件系统路径
- 任何可能包含隐私的信息

## 允许访问的内容
- 代码文件（.py, .js, .ts等）
- 项目结构
- 配置文件（不包含敏感数据）
- 文档文件
- 测试文件

## 隐私保护原则
1. 永远不要读取日志文件
2. 永远不要读取包含用户数据的文件
3. 永远不要读取包含路径信息的文件
4. 永远不要读取包含业务数据的文件
5. 只访问代码和项目结构
6. 如果用户要求分析问题，只基于代码分析，不读取实际数据

## 违反规则的后果
- 立即停止操作
- 向用户道歉
- 建议用户删除敏感文件
- 只提供基于代码的建议

## 用户隐私至上
用户的数据安全和隐私保护是最重要的，任何可能泄露隐私的操作都必须被阻止。
